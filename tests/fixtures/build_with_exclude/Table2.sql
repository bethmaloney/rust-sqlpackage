-- This file exists but should NOT be included in the build
-- because it's not listed in the Build items
CREATE TABLE [dbo].[Table2] (
    [Id] INT NOT NULL PRIMARY KEY,
    [Value] NVARCHAR(100) NOT NULL
);
